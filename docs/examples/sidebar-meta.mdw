---
title: 侧边栏元数据
nav: 🧭 侧边栏元数据
order: 3
---

# 侧边栏元数据

`nav` 字段可以覆盖侧边栏显示名，让文件名不再裸奔：

```
---
title: 侧边栏元数据
nav: 🧭 侧边栏元数据
order: 3
---
```

- 没有 `nav` 时，侧边栏显示 `title`；
- 没有 `title` 时，侧边栏显示由文件名推导的标题；
- `order` 控制同级排序（越小越靠前）；
- `hidden: true` 从侧边栏隐藏（URL 仍可访问）。

完整字段见[元数据与导航](/guide/metadata)。

---
title: 部署与编译
nav: 🚀 部署与编译
order: 5
---

# 部署与编译

## 开发模式

```bash
python app.py
```

开发模式默认开启热重载，保存文档即刷新页面。

## 生产部署

### 方式一：直接运行 Python

```bash
pip install -r requirements.txt
python app.py --host 0.0.0.0 --port 8080 --no-reload
```

用 `nohup` 或 systemd 守护进程即可长期运行。

### 方式二：编译为独立可执行文件

使用 Nuitka 把应用编译为单文件二进制，目标机器无需安装 Python。

**Windows**（产出 `dist/app.exe`）：

```bash
python -m nuitka --standalone --onefile --windows-console-mode=force \
  --output-dir=dist \
  --include-data-dir=mdw/templates=mdw/templates \
  --include-package=pygments --include-package=pygments.lexers \
  --include-package=pygments.formatters --include-package=pygments.styles \
  --include-package=yaml --assume-yes-for-downloads app.py
```

**Linux**（产出 `dist/app.bin`，在 Linux 环境执行）：

```bash
python -m nuitka --standalone --onefile --output-dir=dist \
  --include-data-dir=mdw/templates=mdw/templates \
  --include-package=pygments --include-package=pygments.lexers \
  --include-package=pygments.formatters --include-package=pygments.styles \
  --include-package=yaml --assume-yes-for-downloads app.py
```

运行编译产物：直接执行 `app.exe` / `app` 即可。**无需手动准备任何文件**——
编译时已将 `docs/`（教程）、`static/`（主题）、默认 `config/site.yaml` 打包进
二进制，首次运行自动解压到程序同目录；用户已有内容不会被覆盖。

### 方式三：build.py 脚本

项目自带 `build.py` 打包脚本（编译前自动打包内置资源）：

```bash
python build.py --bundle   # 仅重新打包内置资源（docs/static/config → mdw/_bundle_assets.py）
python build.py            # 打包资源 + 编译单文件
python build.py --install  # 打包资源 + 编译 + 创建发布包
```

## 反向代理（可选）

MDW 服务器本身就是 HTTP 服务，可置于 Nginx / Caddy 之后：

```nginx
server {
    listen 80;
    location / {
        proxy_pass http://127.0.0.1:8080;
    }
}
```

## 安全建议

- 修改 `config/site.yaml` 中的 `api.token` 为强密码
- 管理后台 `/admin` 与 `/api/*` 默认不设访问限制，生产环境建议在
  反向代理层加访问控制（如 IP 白名单、Basic Auth）
- 如需 HTTPS，交由反向代理处理

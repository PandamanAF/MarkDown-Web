---
title: 元数据与导航
nav: 📌 元数据与导航
order: 2
---

# 元数据与导航

在文档文件头部使用 `---` 包裹的 YAML 风格元数据，即可定制页面信息与侧边栏导航：

```
---
title: 页面标题
nav: 侧边栏显示名
parent: /guide
order: 1
hidden: false
icon: 📌
collapsed: true
---
```

## 字段说明

| 字段        | 作用 |
| ----------- | ---- |
| `title`     | 页面标题（浏览器标签 + 侧边栏默认显示名） |
| `nav`       | 侧边栏显示名。例如 `nav: 快速上手` 让侧边栏显示中文名，方便阅读 |
| `parent`    | 挂载到指定父级路由，如 `parent: /guide`。可跨目录、任意层级 |
| `order`     | 同级排序数字，越小越靠前（目录默认 0，文件默认 99） |
| `hidden`    | 设为 `true` 时从侧边栏隐藏（URL 仍可访问） |
| `icon`      | 侧边栏前缀图标（任意文字 / emoji） |
| `collapsed` | 设为 `true` 时该分组默认收起（不影响当前所在分组的展开） |

## 导航层级

- **默认规则**：目录层级天然形成侧边栏层级，嵌套**无上限**，文档编写者
  通常只需写 `title`，其余自动识别。
- **`nav`**：覆盖侧边栏显示名，适合把英文文件名显示为中文。
- **`parent`**：把任意页面挂到任意分组下。例如本页文档
  `docs/examples/basic.mdw` 物理上位于 `examples/` 目录，
  但可以通过 `parent: /guide` 把入口挂到"编写指南"分组中，
  实现跨目录组织导航。
- **`hidden`**：隐藏的页面仍可通过 URL 访问。

::: note
元数据也向扩展开放：`FrontMatterExtension.get_meta()` 可读取当前页面的元数据，
你可以据此实现自定义行为。
:::

---
title: 语法基础
nav: ✍️ 语法基础
order: 1
---

# 语法基础

MDW 文档使用 Markdown 语法，并扩展了**自定义块**等能力。

## Markdown 基础

### 标题

```md
# 一级标题
## 二级标题
### 三级标题
```

### 文本样式

```md
**加粗**   *斜体*   ***加粗斜体***   `行内代码`
```

### 列表

```md
- 无序项目
- 另一个项目

1. 有序项目
2. 第二个
```

### 链接与图片

```md
[显示文字](https://example.com)

![图片描述](_assets/demo.svg)
```

### 引用与分割线

```md
> 这是一段引用

---
```

### 代码块

````md
```python
def hello():
    print("Hello MDW!")
```
````

带语言标注的代码块会自动高亮（需安装 `pygments`）。

## MDW 自定义块

用 `:::` 包裹的块可以渲染为提示框、卡片等特殊样式：

```
::: note
这是一个提示块。
:::

::: warning
这是一个警告块。
:::
```

效果：

::: note
这是一个提示块。
:::

::: warning
这是一个警告块。
:::

### 自定义块类型

| 类型 | 用途 |
| ---- | ---- |
| `note`      | 普通提示 |
| `tip`       | 技巧建议 |
| `warning`   | 警告提醒 |
| `important` | 重要说明 |
| `card`      | 外部 HTML 模板卡片 |
| `page-card` | 页面跳转卡片 |

### 带变量的自定义块

块后可直接写 `key="值"` 覆盖模板变量：

```
::: card title="📇 卡片标题" accent="#e91e63"
卡片内容。
:::
```

详见[自定义样式](/guide/styles)。

## 特殊用法

### 转义 Markdown

需要显示特殊符号时，用反斜杠转义：

```md
\*不是斜体\*  \#不是标题
```

### 标题锚点

每个标题自动生成锚点，点击标题左侧 `#` 区域可复制直达链接。

## 总结

| 功能 | 语法 |
| ---- | ---- |
| 标题 | `# 标题` |
| 加粗 | `**文字**` |
| 斜体 | `*文字*` |
| 引用 | `> 内容` |
| 列表 | `- 内容` |
| 链接 | `[文字](地址)` |
| 图片 | `![描述](地址)` |
| 代码块 | 三个反引号 |
| 自定义块 | `::: 类型` |

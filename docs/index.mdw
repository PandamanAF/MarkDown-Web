---
title: 首页
---

# 欢迎使用 MDW

MDW（**MarkDown Web**）是一个轻量级文档建站框架：把 `docs/` 目录下的
`.md` / `.mdw` 文件直接渲染为响应式网站，文档即网站。

## 核心能力

- 📝 **类 Markdown 语法** — 支持标准 Markdown + 自定义块（提示框 / 卡片 / 外部模板）
- 📂 **自动路由** — 文档目录层级即 URL 结构，嵌套无上限
- 🧭 **元数据驱动导航** — `nav` / `parent` / `order` / `hidden` 定制侧边栏，无需写代码
- 🎨 **主题可换肤** — 修改 `static/style.css` 即可整体换装，浅色 / 暗色自动适配
- 🧩 **模板覆盖** — 页面骨架为外部模板，改头部 / 页脚 / 导航样式不动代码
- 🔌 **扩展系统** — Parser / Renderer / Middleware，按需扩展
- 🔄 **热重载** — 保存文档即刷新页面（开发模式）
- 🤖 **管理 API** — `/api/*` 提供路由列表、页面渲染、文件上传下载、热重载

## 开始探索

::: page-card url="/guide/quickstart" title="⚡ 快速开始" icon="🚀"
五分钟跑起来：安装、运行、写第一篇文章。
:::

::: page-card url="/guide/" title="📖 编写指南" icon="📚"
语法、元数据、样式、模板、扩展、部署，全功能参考。
:::

::: page-card url="/examples/" title="🧪 示例集" icon="✨"
各功能开箱即用的完整示例。
:::

::: page-card url="/api/" title="🤖 管理 API" icon="🔌"
HTTP 接口参考与通用客户端示例。
:::

::: note
文档头部可用 `---` 包裹的元数据定制页面与导航，详见[元数据与导航](/guide/metadata)。
:::
